# Deskone v2 bootstrap

This Supabase folder is intentionally reset for v2.

- `legacy-v1/` contains archived v1 migrations and functions for reference only.
- `migrations/` is the new clean migration track for v2.
- `functions/` is the new clean edge function track for v2.

Do not apply the archived v1 migrations to the v2 project.
